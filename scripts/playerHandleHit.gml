/// playerHandleHit();
// While being hit
if (isHit)
{
    if place_meeting(x,bbox_bottom+16,objBitManYoku) && ground y += 2
    if (hitTimer >= hitTime)
    {
        isHit = false;
        hitTimer = 0;
        global.hydroHit = false
        hitLock = lockPoolRelease(hitLock);
        
        if (iFrames != 0)
        {
            iFrames = 60 * (1 + (global.sturdyHelmet * 0.5));
        }
    }
}
